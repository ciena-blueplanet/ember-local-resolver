Local version of \{{x-both}} was used from <code>pods/pod-local-wins/-components/x-both</code>.

<hr>

When there is both a local match and global match, the local version wins. The local component can then invoke the global version:

<hr>

{{x-both}}